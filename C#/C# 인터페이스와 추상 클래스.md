# 인터페이스와 추상 클래스
인터페이스는 interface 키워드를 이용해 선언
```
interface 인터페이스이름
{
	반환_형식 메소드이름1( 매개변수_목록 );
	반환_형식 메소드이름2( 매개변수_목록 );
	// ...
}
```
메소드, 이벤트, 인덱서, 프로퍼티만을 가질 수 있음  
접근 제한 한정자의 사용이 불가능하며 기본적으로 public으로 선언됨  
상속받는 클래스의 인스턴스를 만들 수 있음  